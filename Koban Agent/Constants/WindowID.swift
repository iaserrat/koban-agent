import Foundation

/// Identifiers for the app's windows: used as the extended window's frame autosave name so its
/// size and position persist across launches. The single home for these literals (see CLAUDE.md).
enum WindowID {
    static let main = "koban.main"
}
