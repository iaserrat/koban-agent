import Foundation

// MARK: - FeedLimits

/// How much recent history the menu-bar UI shows. The single home for these counts.
enum FeedLimits {
    static let events = 50
    static let findings = 50
}
